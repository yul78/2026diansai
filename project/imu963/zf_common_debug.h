#ifndef ZF_COMMON_DEBUG_H
#define ZF_COMMON_DEBUG_H

/* The original driver uses these only for diagnostics. */
#define zf_assert(condition) ((void) 0)
#define zf_log(level, message) ((void) 0)

#endif
